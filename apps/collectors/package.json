{
  "name": "@f1-job-radar/collectors",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/bundle.js",
  "scripts": {
    "build": "tsc -b && npm run bundle",
    "bundle": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/bundle.js --external:@azure/functions-core --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "watch": "tsc -b -w",
    "start": "func start",
    "test": "vitest run"
  },
  "dependencies": {
    "@azure/functions": "^4.6.1",
    "@f1-job-radar/db": "*",
    "@f1-job-radar/schema": "*",
    "fast-xml-parser": "^5.9.3"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
